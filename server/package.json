{
  "name": "srv-google-shop",
  "version": "1.0.0",
  "description": "server for google shop app",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start_dev": "nodemon -r dotenv/config app.js"
  },
  "author": "wet.cinnamon",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "dotenv": "^10.0.0",
    "nodemon": "^2.0.9"
  }
}
